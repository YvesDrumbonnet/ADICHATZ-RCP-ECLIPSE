/**********************************************************************************************
 * This class was automatically generated by Adichatz tools on Fri Feb 07 15:54:07 CET 2020
 *
 * 				+----------------------------------------+
 * 				| BE CAREFUL BEFORE UPDATING THIS CLASS! |
 * 				+----------------------------------------+
 *
 * Changes will be transient up to next generation process.
 * **********************************************************************************************/
package org.adichatz.studio.gencode.editor;

import org.adichatz.engine.common.AdichatzApplication;
import org.adichatz.engine.controller.IContainerController;
import org.adichatz.engine.controller.field.TableColumnController;
import org.adichatz.engine.core.ATabularCore;
import org.adichatz.engine.extra.AdiResourceURI;
import org.adichatz.engine.extra.ColumnViewerSorter;
import org.adichatz.engine.plugin.AdiContext;
import org.adichatz.engine.tabular.ATabularContentProvider;
import org.adichatz.engine.viewer.NativeContentProvider;
import org.adichatz.generator.xjc.ScenarioType;
import org.adichatz.studio.xjc.controller.FeaturesTableController;

@AdiResourceURI(URI="adi://org.adichatz.studio/editor/ScenarioEditor")
public class ScenarioEditorScenariosTable extends ATabularCore<ScenarioType> {
	// the TableColumnController typeTCTC.
	private TableColumnController typeTCTC;
	// the TableColumnController scenarioClassNameTCTC.
	private TableColumnController scenarioClassNameTCTC;
	public ScenarioEditorScenariosTable(final AdiContext context, IContainerController parentController) {
		super(context);
		tabularController = new FeaturesTableController<ScenarioType>("scenariosTable", parentController, this) {
			@Override
			public void createControl() {
				if (isValid()) {
					beanClass = ScenarioType.class;
				}
				super.createControl();
				if (isValid()) {
					ATabularContentProvider contentProvider$1 = new NativeContentProvider("scenario");
					contentProvider$1.setTabularController(tabularController);
					getControl().setLayoutData("h 0:n:n, w 0:n:n");
				}
			}
		};
		tabularController.setPluginResources(AdichatzApplication.getPluginResources("org.adichatz.studio"));
		coreController = tabularController;
		
		// Creates control for TableColumnController typeTCTC
		typeTCTC = new TableColumnController<ScenarioType>("typeTC", tabularController) {
			@Override
			public void createControl() {
				super.createControl();
				if (isValid()) {
					getControl().getColumn().setImage(AdichatzApplication.getInstance().getImage("org.adichatz.studio", "IMG_SCENARIO.png"));
					getControl().getColumn().setText(AdichatzApplication.getInstance().getMessage("org.adichatz.studio", "scenario", "type"));
					setColumnViewerSorter(ColumnViewerSorter.ASC);
				}
			}
			@Override
			public Object getValue(ScenarioType row) {
				return row.getType();
			}
			@Override
			public String getColumnText(ScenarioType row) {
				return  null == row.getType() ? null : row.getType().value();
			}
		};
		// Creates control for TableColumnController scenarioClassNameTCTC
		scenarioClassNameTCTC = new TableColumnController<ScenarioType>("scenarioClassNameTC", tabularController) {
			@Override
			public void createControl() {
				super.createControl();
				if (isValid()) {
					getControl().getColumn().setText(AdichatzApplication.getInstance().getMessage("org.adichatz.studio", "scenario", "scenarioClassName"));
					setColumnViewerSorter(ColumnViewerSorter.ASC);
				}
			}
			@Override
			public String getValue(ScenarioType row) {
				return row.getScenarioClassName();
			}
			@Override
			public String getColumnText(ScenarioType row) {
				return row.getScenarioClassName();
			}
		};
	}
	
	@Override
	public Object getId(Object element) {
		return null;
	}
}