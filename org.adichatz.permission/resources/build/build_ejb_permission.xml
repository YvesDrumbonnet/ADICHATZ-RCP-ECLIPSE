<project name="MyProject" default="init" basedir="../..">
	<description>simple example build file</description>
	<!-- set global properties for this build -->
	<property name="bin" location="bin" />
	<property name="resources.build.dir" location="resources/build" />
	<property name="dest.jar.dir" location="${basedir}/resources/build" />
	<property name="deploy.dir" location="C:/ADICHATZ/applicationServers/wildfly-preview-24.0.1.Final/standalone/deployments"/>

	<target name="deployEJB">
		<jar destfile="${dest.jar.dir}/PermissionServiceEJB.jar" manifest="${resources.build.dir}/MANIFEST.MF">
			<fileset dir="${bin}">
				<include name="org/adichatz/permission/ejb/*.*" />
			</fileset>
		</jar>
		<copy file="${dest.jar.dir}/PermissionServiceEJB.jar" todir="${deploy.dir}"/>
	</target>
	
	<target name="init">
		<!-- Create the time stamp -->
		<tstamp />
		<!-- Create the build directory structure used by compile -->
		<echo>Time ${DSTAMP} ${TSTAMP}</echo>
		<antcall target="deployEJB"/>
	</target>

</project>