/**********************************************************************************************
 * This class was automatically generated by Adichatz tools on Tue Jul 07 18:46:55 CEST 2020
 *
 * 				+----------------------------------------+
 * 				| BE CAREFUL BEFORE UPDATING THIS CLASS! |
 * 				+----------------------------------------+
 *
 * Changes will be transient up to next generation process.
 * **********************************************************************************************/
package org.adichatz.jpa.gencode.query;

import java.util.function.BooleanSupplier;
import javax.inject.Inject;
import net.miginfocom.swt.MigLayout;
import org.adichatz.engine.common.AdichatzApplication;
import org.adichatz.engine.controller.IContainerController;
import org.adichatz.engine.controller.nebula.PShelfController;
import org.adichatz.engine.controller.nebula.PShelfItemController;
import org.adichatz.engine.controller.utils.ElementaryAccessibility;
import org.adichatz.engine.core.EntityManagerCore;
import org.adichatz.engine.extra.AdiResourceURI;
import org.adichatz.engine.plugin.AdiContext;
import org.adichatz.engine.renderer.AdiFormToolkit;
import org.adichatz.jpa.query.custom.CrossReferenceBarController;

@AdiResourceURI(URI="adi://org.adichatz.jpa/query/QueryToolContainer")
public class QueryToolContainerCrossReferences extends EntityManagerCore {
	// the CrossReferenceBarController crossReferenceButtonBarCMPS.
	protected CrossReferenceBarController crossReferenceButtonBarCMPS;
	@Inject
	protected AdiFormToolkit toolkit;
	/**
	 * Creates QueryToolContainerCrossReferences.
	 *
	 * @param context
	 *            The context of the root controller
	 * @param core controller
	 *            The collection controller linked to the class
	 */
	public QueryToolContainerCrossReferences(final AdiContext context, IContainerController parentController) {
		super(context);
		// do not create contents when context is null because that means that class is invoked from dynamic part
		if (null != context) {
			coreController = new PShelfItemController("crossReferences", parentController, this) {
				@Override
				public void initialize() {
					BooleanSupplier evaluatorVISIBLE;
					evaluatorVISIBLE = new BooleanSupplier() {
						public boolean getAsBoolean() {
							return !((CrossReferenceBarController) getFromRegister("crossReferenceButtonBar")).isEmpty();
						}
					};
					addAccessibility(new ElementaryAccessibility(ElementaryAccessibility.ACCESS_ATTRIBUTE.VISIBLE, evaluatorVISIBLE));
					super.initialize();
				}
				@Override
				public void createControl() {
					super.createControl();
					if (isValid()) {
						getItem().setImage(toolkit.getRegisteredImage("IMG_CROSS_REFERENCE"));
						getItem().setText(AdichatzApplication.getInstance().getMessage("org.adichatz.jpa", "adichatzJpa", "table.crossReferences"));
						getComposite().setLayout(new MigLayout("ins 0, wrap 1","grow, fill",null));
					}
				}
			};
			if (!((PShelfController) parentController).isDelayed())
				createContents();
		} else
			coreController = parentController;
	}

	/**
	 * creates contents for controller
	 */
	public void createContents(){
		// Creates control for CrossReferenceBarController crossReferenceButtonBarCMPS
		crossReferenceButtonBarCMPS = new CrossReferenceBarController("crossReferenceButtonBar", coreController, this);
	}
}