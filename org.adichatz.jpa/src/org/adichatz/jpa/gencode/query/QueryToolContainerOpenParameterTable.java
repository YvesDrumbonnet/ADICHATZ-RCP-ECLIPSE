/**********************************************************************************************
 * This class was automatically generated by Adichatz tools on Fri Feb 21 14:35:34 CET 2020
 *
 * 				+----------------------------------------+
 * 				| BE CAREFUL BEFORE UPDATING THIS CLASS! |
 * 				+----------------------------------------+
 *
 * Changes will be transient up to next generation process.
 * **********************************************************************************************/
package org.adichatz.jpa.gencode.query;

import org.adichatz.engine.common.AdichatzApplication;
import org.adichatz.engine.controller.IContainerController;
import org.adichatz.engine.controller.field.TableColumnController;
import org.adichatz.engine.core.ATabularCore;
import org.adichatz.engine.extra.AdiResourceURI;
import org.adichatz.engine.extra.ColumnViewerSorter;
import org.adichatz.engine.plugin.AdiContext;
import org.adichatz.engine.tabular.ATabularContentProvider;
import org.adichatz.engine.viewer.NativeContentProvider;
import org.adichatz.jpa.query.custom.OpenClauseParameterTableController;
import org.adichatz.jpa.wrapper.QueryParameterWrapper;

@AdiResourceURI(URI="adi://org.adichatz.jpa/query/QueryToolContainer")
public class QueryToolContainerOpenParameterTable extends ATabularCore<QueryParameterWrapper> {
	// the TableColumnController promptTC.
	private TableColumnController promptTC;
	// the TableColumnController expressionTC.
	private TableColumnController expressionTC;
	public QueryToolContainerOpenParameterTable(final AdiContext context, IContainerController parentController) {
		super(context);
		tabularController = new OpenClauseParameterTableController<QueryParameterWrapper>("openParameterTable", parentController, this) {
			@Override
			public void createControl() {
				if (isValid()) {
					beanClass = QueryParameterWrapper.class;
				}
				super.createControl();
				if (isValid()) {
					ATabularContentProvider contentProvider$1 = new NativeContentProvider("parameter");
					contentProvider$1.setTabularController(tabularController);
					getControl().setLayoutData("h n:100:n, w 300:300:n");
				}
			}
			@Override
			public void endLifeCycle() {
				delegateAfterEndLifeCycleListener = true;
				super.endLifeCycle();
				if (isValid()) {
					refresh();
				}
			}
		};
		tabularController.setPluginResources(AdichatzApplication.getPluginResources("org.adichatz.jpa"));
		coreController = tabularController;
		
		// Creates control for TableColumnController promptTC
		promptTC = new TableColumnController<QueryParameterWrapper>("prompt", tabularController) {
			@Override
			public void createControl() {
				super.createControl();
				if (isValid()) {
					getControl().getColumn().setText(AdichatzApplication.getInstance().getMessage("org.adichatz.jpa", "parameter", "prompt"));
					setColumnViewerSorter(ColumnViewerSorter.ASC);
				}
			}
			@Override
			public String getValue(QueryParameterWrapper row) {
				return row.getPrompt();
			}
			@Override
			public String getColumnText(QueryParameterWrapper row) {
				return row.getPrompt();
			}
		};
		// Creates control for TableColumnController expressionTC
		expressionTC = new TableColumnController<QueryParameterWrapper>("expression", tabularController) {
			@Override
			public void createControl() {
				super.createControl();
				if (isValid()) {
					getControl().getColumn().setText(AdichatzApplication.getInstance().getMessage("org.adichatz.jpa", "parameter", "expression"));
				}
			}
			@Override
			public String getValue(QueryParameterWrapper row) {
				return getColumnText(row);
			}
			@Override
			public String getColumnText(QueryParameterWrapper row) {
				return row.getColumnText();
			}
		};
	}
	
	@Override
	public Object getId(Object element) {
		return null;
	}
}