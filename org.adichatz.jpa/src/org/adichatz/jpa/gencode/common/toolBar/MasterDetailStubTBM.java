/**********************************************************************************************
 * This class was automatically generated by Adichatz tools on Tue Jul 07 12:30:02 CEST 2020
 *
 * 				+----------------------------------------+
 * 				| BE CAREFUL BEFORE UPDATING THIS CLASS! |
 * 				+----------------------------------------+
 *
 * Changes will be transient up to next generation process.
 * **********************************************************************************************/
package org.adichatz.jpa.gencode.common.toolBar;

import org.adichatz.engine.action.AAction;
import org.adichatz.engine.controller.AWidgetController;
import org.adichatz.engine.controller.IContainerController;
import org.adichatz.engine.controller.collection.FormPageController;
import org.adichatz.engine.controller.collection.ManagedToolBarController;
import org.adichatz.engine.controller.collection.SashFormController;
import org.adichatz.engine.core.AIncludeCore;
import org.adichatz.engine.extra.AdiResourceURI;
import org.adichatz.engine.plugin.AdiContext;
import org.adichatz.engine.plugin.ParamMap;
import org.eclipse.swt.custom.SashForm;
import org.eclipse.swt.widgets.Control;

@AdiResourceURI(URI="adi://org.adichatz.jpa/common.toolBar/MasterDetailStubTBM")
public class MasterDetailStubTBM extends AIncludeCore {
	// the ManagedToolBarController managedToolBarTB.
	protected ManagedToolBarController managedToolBarTB;
	/**
	 * Instantiates a new MasterDetailStubTBM.
	 * 
	 * This constructor could be used by Column parameter. (see org.adichatz.jpa)
	 * 
	 * @param parentContext
	 *            the parent context
	 * @param parentController
	 *            the parent controller
	 */
	public MasterDetailStubTBM(AdiContext parentContext, IContainerController parentController) {
		coreController = parentController;
	}

	/**
	 * Creates the include MasterDetailStubTBM.
	 *
	 * @param context
	 *            The context of the root controller
	 * @param parent controller
	 *            The collection parentController
	 * @param id
	 *            The include id
	 * @param parentContext
	 *            The parent context
	 */
	public MasterDetailStubTBM(ParamMap paramMap, IContainerController parentController, String id, AdiContext parentContext) {
		super(paramMap, parentController, id, parentContext);
		coreController = parentController;
	}

	/**
	 * creates contents
	 */
	public void createContents(){
		// Creates control for ManagedToolBarController managedToolBarTB
		managedToolBarTB = new ManagedToolBarController("managedToolBar", coreController, this);
	}
	
	//* *****************
	//* Additional code *
	//*******************
	private SashForm sashForm;
	protected FormPageController getMasterDetailPageController() {
	    return (FormPageController) coreController.getParentController();
	}
	protected Control getMasterControl() {
	    return getSashForm().getChildren()[0];
	}
	protected Control getDetailControl() {
	    return getSashForm().getChildren()[1];
	}
	protected SashForm getSashForm() {
		if (null == sashForm)
	    	for (AWidgetController controller : getMasterDetailPageController().getChildControllers())
	        	if (controller instanceof SashFormController)
	            	sashForm = ((SashFormController) controller).getControl();
	    return sashForm;
	}
	protected void expand(AAction action, Control control) {
	}
	protected void setImages(SashForm sashForm) {
	}
}