/**********************************************************************************************
 * This class was automatically generated by Adichatz tools on Fri Feb 21 12:39:52 CET 2020
 *
 * 				+----------------------------------------+
 * 				| BE CAREFUL BEFORE UPDATING THIS CLASS! |
 * 				+----------------------------------------+
 *
 * Changes will be transient up to next generation process.
 * **********************************************************************************************/
package org.adichatz.jpa.gencode.common.toolBar;

import java.util.function.BooleanSupplier;
import org.adichatz.common.ejb.util.IEntityConstants;
import org.adichatz.engine.controller.IContainerController;
import org.adichatz.engine.controller.nebula.ActionPGroupToolItemController;
import org.adichatz.engine.controller.nebula.PGroupController;
import org.adichatz.engine.controller.utils.ElementaryAccessibility;
import org.adichatz.engine.core.AIncludeCore;
import org.adichatz.engine.extra.AdiResourceURI;
import org.adichatz.engine.plugin.AdiContext;
import org.adichatz.engine.plugin.ParamMap;
import org.adichatz.jpa.action.DeleteEntityAction;
import org.adichatz.jpa.action.DeleteEntityAndCloseAction;
import org.adichatz.jpa.action.RefreshEntityAction;

@AdiResourceURI(URI="adi://org.adichatz.jpa/common.toolBar/PGroupDetailTBM")
public class PGroupDetailTBM extends AIncludeCore {
	// the PGroupController masterPGroupPGRP.
	protected PGroupController masterPGroupPGRP;
	// the PGroupController masterPGroupPGRP.
	// the ActionPGroupToolItemController refreshEntityItemPGTI.
	private ActionPGroupToolItemController refreshEntityItemPGTI;
	// the ActionPGroupToolItemController deleteEntityAndCloseItemPGTI.
	private ActionPGroupToolItemController deleteEntityAndCloseItemPGTI;
	// the ActionPGroupToolItemController deleteEntityItemPGTI.
	private ActionPGroupToolItemController deleteEntityItemPGTI;
	/**
	 * Instantiates a new PGroupDetailTBM.
	 * 
	 * This constructor could be used by Column parameter. (see org.adichatz.jpa)
	 * 
	 * @param parentContext
	 *            the parent context
	 * @param parentController
	 *            the parent controller
	 */
	public PGroupDetailTBM(AdiContext parentContext, IContainerController parentController) {
		coreController = parentController;
	}

	/**
	 * Creates the include PGroupDetailTBM.
	 *
	 * @param context
	 *            The context of the root controller
	 * @param parent controller
	 *            The collection parentController
	 * @param id
	 *            The include id
	 * @param parentContext
	 *            The parent context
	 */
	public PGroupDetailTBM(ParamMap paramMap, IContainerController parentController, String id, AdiContext parentContext) {
		super(paramMap, parentController, id, parentContext);
		coreController = parentController;
	}

	/**
	 * creates contents
	 */
	public void createContents(){
		// control is referenced by content of widgetWrapper.getRef masterPGroupPGRP
		masterPGroupPGRP = (PGroupController) coreController.getParentController();
		// Creates control for ActionPGroupToolItemController refreshEntityItemPGTI
		refreshEntityItemPGTI = new ActionPGroupToolItemController("refreshEntityItem", masterPGroupPGRP, this) {
			@Override
			public void initialize() {
				getParamMap().put("CONTROLLER", context.getParam("CONTROLLER"));
				super.initialize();
			}
			@Override
			public void createControl() {
				super.createControl();
				if (isValid()) {
					setAction(new RefreshEntityAction());
				}
			}
		};
		// Creates control for ActionPGroupToolItemController deleteEntityAndCloseItemPGTI
		deleteEntityAndCloseItemPGTI = new ActionPGroupToolItemController("deleteEntityAndCloseItem", masterPGroupPGRP, this) {
			@Override
			public void initialize() {
				getParamMap().put("CONTROLLER", context.getParam("CONTROLLER"));
				getParamMap().put("TOOL_BAR_TYPE", context.getParam("TOOL_BAR_TYPE"));
				BooleanSupplier evaluatorVALID;
				evaluatorVALID = new BooleanSupplier() {
					public boolean getAsBoolean() {
						return "DETAIL".equals(((String) context.getParam("TOOL_BAR_TYPE")));
					}
				};
				addAccessibility(new ElementaryAccessibility(ElementaryAccessibility.ACCESS_ATTRIBUTE.VALID, evaluatorVALID));
				super.initialize();
			}
			@Override
			public void createControl() {
				super.createControl();
				if (isValid()) {
					setAction(new DeleteEntityAndCloseAction());
				}
			}
		};
		// Creates control for ActionPGroupToolItemController deleteEntityItemPGTI
		deleteEntityItemPGTI = new ActionPGroupToolItemController("deleteEntityItem", masterPGroupPGRP, this) {
			@Override
			public void initialize() {
				getParamMap().put("CONTROLLER", context.getParam("CONTROLLER"));
				getParamMap().put("TOOL_BAR_TYPE", context.getParam("TOOL_BAR_TYPE"));
				BooleanSupplier evaluatorVALID;
				evaluatorVALID = new BooleanSupplier() {
					public boolean getAsBoolean() {
						return genCode.checkPrivilege(IEntityConstants.REMOVE) && "CHILD".equals(((String) context.getParam("TOOL_BAR_TYPE")));
					}
				};
				addAccessibility(new ElementaryAccessibility(ElementaryAccessibility.ACCESS_ATTRIBUTE.VALID, evaluatorVALID));
				super.initialize();
			}
			@Override
			public void createControl() {
				super.createControl();
				if (isValid()) {
					setAction(new DeleteEntityAction());
				}
			}
		};
	}
}